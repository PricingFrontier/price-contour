# target-cpu is set via environment variable to allow CI overrides.
# For local development, add to your shell profile:
#   export RUSTFLAGS="-C target-cpu=native"
# Or run: RUSTFLAGS="-C target-cpu=native" cargo build --release
